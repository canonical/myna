# List of source files containing translatable strings, one per line,
# relative to the crate root (client/myna-hud). Keep in sync with the files
# that call gettext. Extracted with:
#   xgettext --from-code=UTF-8 --keyword=gettext --add-comments=TRANSLATORS \
#     --output=po/myna.pot --files-from=po/POTFILES.in
src/lab.rs
../myna-desktop/src/indicator/dbus.rs
