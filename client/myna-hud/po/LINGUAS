# List of translated languages (one language code per line). No translations
# exist yet — add each locale's code here (e.g. `fr`, `pt_BR`) alongside its
# `<lang>.po` file, created from po/myna.pot with `msginit`.
