fn main() {
    // T100 scaffold: the adw::Application shell and the mode dispatch
    // (hosted default / `--lab` / `--serve-dbus`) land with the renderer-app
    // phase (T124, plan.md Phase C).
    println!("myna-hud {}", env!("CARGO_PKG_VERSION"));
}
