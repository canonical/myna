using Gtk 4.0;
using Adw 1;

template $ApplyDialog: Adw.AlertDialog {
  heading: _("Review backend changes");
  body: _("Review the requested changes, exact argv, privilege explanation, and restart impact below.");
  default-response: "apply";
  close-response: "cancel";

  responses [
    cancel: _("Cancel"),
    apply: _("Apply"),
  ]

  extra-child: Gtk.ScrolledWindow {
    min-content-height: 320;
    propagate-natural-height: true;

    child: Gtk.Label confirmation_label {
      wrap: true;
      selectable: true;
      xalign: 0;
      yalign: 0;
    };
  };
}
