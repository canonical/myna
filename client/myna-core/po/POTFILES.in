# List of source files containing translatable strings, one per line,
# relative to the crate root (client/myna-core). Keep in sync with the files
# that call `tr`. Extracted with:
#   xgettext --from-code=UTF-8 --keyword=gettext --keyword=tr \
#     --add-comments=TRANSLATORS \
#     --output=po/myna-core.pot --files-from=po/POTFILES.in
src/failure.rs
