# List of source files containing translatable strings, one per line,
# relative to the crate root (client/myna-desktop). Keep in sync with the files
# that call gettext. Extracted with:
#   xgettext --language=C --from-code=UTF-8 --keyword=gettext --add-comments=TRANSLATORS \
#     --output=po/myna-desktop.pot --files-from=po/POTFILES.in
src/indicator/mod.rs
src/indicator/notify.rs
src/controller.rs
src/shortcut/retry.rs
