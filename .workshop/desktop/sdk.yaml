# In-project SDK: desktop build + runtime dependencies
#
name: desktop
summary: GTK4/libadwaita headers, Blueprint compiler, IBus daemon, xdg-desktop-portal, and D-Bus session bus for desktop Rust clients.
